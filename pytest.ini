[pytest]
# importlib mode lets `tests/unit/observers/` coexist with `src/observers/`
# without the package-name collision that `prepend` (the default) causes.
addopts = --import-mode=importlib
testpaths = tests
